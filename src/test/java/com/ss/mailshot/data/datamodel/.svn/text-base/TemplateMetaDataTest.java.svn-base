package com.ss.mailshot.data.datamodel;

import org.junit.Test;

/**
 * Created by IntelliJ IDEA.
 * User: jatin
 * Date: 21-Mar-2010
 * Time: 10:00:50
 */
public class TemplateMetaDataTest {



    @Test
    public void testTable(){
        /*TemplateMetaData template = new TemplateMetaData();

		Session session = HibernateUtil.getSessionFactory().openSession();

        Transaction transaction = session.beginTransaction();
        transaction.begin();

        session.saveOrUpdate(template);*/

        /*transaction.commit();

        Criteria criteria = session.createCriteria(TemplateMetaData.class);
        List list = criteria.list();

        System.out.println(list);

        session.close();
		HibernateUtil.shutdown();*/
    }

    @Test
    public void testCreate() {
        /*ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext("mailshot.xml");

        TemplateService templateService = (TemplateService) context.getBean("templateService");

        TemplateMetaData newTemplate = new TemplateMetaData();
        newTemplate.setTemplateCSVFile("Testcsvfile.csv");
        newTemplate.setTemplateName("TestTemplate");
        newTemplate.setTemplateText("This is test template text");

        EMailBean emailBean = new EMailBean();
        emailBean.setToEmailAddress("testAddress@thetest.com");
        emailBean.setCcEmailAddresses("testCCAddress@thetest.com");
        emailBean.setBccEmailAddresses("testBCCAddress@thetest.com");
        emailBean.setReplytoAddress("replyto@thetest.com");
        emailBean.setFromEmailAddress("from@thetest.com");
        emailBean.setSubject("This is a subject");

        newTemplate.setEmailBean(emailBean);

        List<Field> fields = new ArrayList<Field>();
        fields.add( new Field("heading1","type1"));
        fields.add( new Field("heading2","type2"));
        fields.add( new Field("heading3","type3"));
        newTemplate.setFields(fields);

        TemplateMetaData template = templateService.createTemplate(newTemplate);

        assertTrue(template != null);
        System.out.println("Created: " + template);*/
	}

}
